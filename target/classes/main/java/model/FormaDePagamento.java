package main.java.model;


public enum FormaDePagamento {
    CREDITO,
    DEBITO,
    DINHEIRO;

//ENUM limita as opções ao que for declarado na classe, serve para situações que que opções já são fechadas.
//    ex: sexo, forma de pagamento.
}

